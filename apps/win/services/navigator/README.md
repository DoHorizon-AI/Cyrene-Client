# Navigator Windows UI / Navigator Windows 界面

Reserved for a Windows-native Navigator UI module. The browser console lives at `apps/web/services/navigator/`; no native UI package or downloadable bundle exists yet.

此目录预留给 Windows 原生 Navigator UI 模块。浏览器控制台位于 `apps/web/services/navigator/`；当前没有原生 UI 包或可下载 bundle。
