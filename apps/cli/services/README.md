# CLI Service Modules / CLI 服务模块

Reserved roots for per-service CLI command groups: `catalyst/`, `yield/`, `echo/`, `reactor/`, `exchange/`, `navigator/`. Each module should build independently and reuse repository-level contracts and client logic from `packages/`. CLI install/update scripts stay at the `cli/` root.

CLI 各服务命令组的预留目录：`catalyst/`、`yield/`、`echo/`、`reactor/`、`exchange/`、`navigator/`。各模块应可独立构建，并复用仓库根目录 `packages/` 中的契约和客户端逻辑。CLI 安装/更新脚本放在 `cli/` 根目录。

| Module | State |
| --- | --- |
| All six | Reserved; no CLI implementation exists yet. / 全部为预留，尚无 CLI 实现。 |
