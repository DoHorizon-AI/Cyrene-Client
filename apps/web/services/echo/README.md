# Echo Web UI / Echo Web 界面

Reserved for an independently buildable Echo UI module. Evaluation configuration remains integrated in the shared workbench; no standalone package or downloadable bundle exists yet.

此目录预留给可独立构建的 Echo UI 模块。评测配置界面仍集成在共享工作台中；当前没有独立 package 或可下载 bundle。
