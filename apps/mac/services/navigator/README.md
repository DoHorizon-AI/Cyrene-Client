# Navigator macOS UI / Navigator macOS 界面

Reserved for a macOS-native Navigator UI module. The macOS platform is deferred; no implementation exists.

此目录预留给 macOS 原生 Navigator UI 模块。macOS 平台暂缓建设；尚无实现。
